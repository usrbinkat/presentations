# Presentations environment — operational defaults loaded by direnv.
# Override in .env (gitignored) for local customization.

# Default deck for make targets (dev, build, export, routes, lint)
DECK=rekindle-transport-veilid

# Default port for the Slidev dev server
PORT=3030
