// vite.config.ts
import { defineConfig } from 'vite'

export default defineConfig({
  // Shared Vite config for all decks.
  // Per-deck vite.config.ts can extend/override.
  // Intentionally minimal — Slidev provides its own 18 plugins.
})
