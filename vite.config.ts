import { defineConfig } from 'vite'

// Root vite config — intentionally minimal.
// Theme-level plugins (gitMetadataPlugin, sharedAssetsPlugin) are in
// packages/slidev-theme-braincraft/vite.config.ts and loaded automatically
// by Slidev's resolveViteConfigs from theme roots.

export default defineConfig({})
